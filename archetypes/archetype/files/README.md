Archetype for [charmix](https://www.npmjs.com/package/charmix).

## How to use

- Install charmix `npm i -g charmix`
- Add archetype`TODO: add command`
- Go to project directory and run command `charmix use $ARCHETYPE_NAME`
